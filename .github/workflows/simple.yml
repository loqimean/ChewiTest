name: test workflow

on: push

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: echo 'hello!)'

      - name: Show ruby version
        run: |
          ruby -v
          rails -v
