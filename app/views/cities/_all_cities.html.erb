<table>
  <thead>
    <tr>
      <th>City name</th>
      <th colspan="1"></th>
    </tr>
  </thead>

  <tbody>
    <span id="cities-filter-status" data-cities-filter-status=<%= params[:cities_ids] %> disabled></span>
    <% cities.each do |key, filter| %>
      <tr>
        <td><%= link_to '', class: 'cities_filter_items',
                                             data: { disable_with: false } do %>
          <div class="form-check">
            <input class="form-check-input" type="checkbox" value="<%= key %>"
                                        id="flexCheckIndeterminate-<%= key %>">
            <label class="form-check-label" for="flexCheckIndeterminate-<%= key %>">
              <%= City.find(key).name + " (#{filter[:size]})" %>
            </label>
          </div>
        <% end %></td>
      </tr>
    <% end %>
    <tr>
      <td><%= link_to 'Test params', '' %></td>
    </tr>
  </tbody>
</table>
