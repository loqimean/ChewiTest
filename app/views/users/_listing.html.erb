<table id="userListing" class="table">
  <% if users&.exists? %>
    <thead>
      <tr class="row">
        <th scope="col" class="col-xl-4">Name</th>
        <th scope="col" class="col-xl-4">Email</th>
        <th scope="col" class="col-xl-4">Seniority</th>
      </tr>
    </thead>

    <tbody>
      <% users.each do |user| %>
        <tr class="row">
          <td class="col-xl-4"><%= user.name %></td>
          <td class="col-xl-4"><%= user.email %></td>
          <td class="col-xl-4"><%= user.seniority %></td>
        </tr>
      <% end %>


    </tbody>
  <% else %>
    <tr class="text-center">
      <td class="col-xl-12 p-5"><em class="fs-6">Hmm.., it's empty 🤔</em></td>
    </tr>
  <% end %>
</table>
